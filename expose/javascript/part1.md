# Part 1. A Quick Introduction...
1. it will output `20`

2. it will also output `20`

3. you won't want to use `var` since they are visible within the entire function scope, meaning if you initialize a `var` in an `if` statement and that if statement is run, you will be able to see the variable declared as a `var`

4. it will output 20

5. this will output an error message, since `result` is declared with a `let`, and is outside the `if` statement scope.

6. this will result in an error since `const` variable can not be modified

7. this will result in an error from line `9` meaning that line `13` will never be reached

